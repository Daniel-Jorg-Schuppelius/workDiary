APP_NAME=WorkDiary
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

LEGACY_DB_DRIVER=mysql
LEGACY_DB_URL=
LEGACY_DB_HOST=127.0.0.1
LEGACY_DB_PORT=3306
LEGACY_DB_DATABASE=
LEGACY_DB_USERNAME=
LEGACY_DB_PASSWORD=
LEGACY_DB_SOCKET=
LEGACY_DB_CHARSET=latin1
LEGACY_DB_COLLATION=latin1_german2_ci
LEGACY_DB_PREFIX=
LEGACY_MYSQL_ATTR_SSL_CA=
LEGACY_DB_ENCRYPT=no
LEGACY_DB_TRUST_SERVER_CERTIFICATE=true

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=null
SESSION_SECURE_COOKIE=true
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=lax

# API-Token-Ablauf in Minuten (leer = kein Ablauf). Für höhere Sicherheit z. B. 43200 (30 Tage).
SANCTUM_TOKEN_EXPIRATION=

# CSP Stufe 1 (Default AN seit 2026-07-14): Pro-Request-Nonce statt
# 'unsafe-inline' in script-src. Bei CSP-Konsolenfehlern (nicht-nonctes
# Inline-Script) auf false zurückschalten und die Stelle auf @cspNonce umstellen
# (Checkliste: WorkDiary-Architecture/security/hardening-2026-06.md).
CSP_SCRIPT_NONCE=true

# CSP Stufe 2 (Default AN seit 2026-07-14): Alpine im @alpinejs/csp-Build →
# 'unsafe-eval' entfällt aus script-src. Steuert AUCH den Vite-Build-Switch
# (vite.config.js): nach JEDEM Umschalten zwingend `npm run build`, sonst passt
# der CSP-Header nicht zum Bundle.
ALPINE_CSP_BUILD=true

# CVD-Meldekanal /.well-known/security.txt (RFC 9116, CRA-Welle 1): E-Mail
# oder URL für Schwachstellenmeldungen. Leer = Endpunkt liefert 404.
# Optional: SECURITY_TXT_POLICY (CVD-Policy-URL), SECURITY_TXT_LANGUAGES,
# SECURITY_TXT_EXPIRES_DAYS (Default 180).
SECURITY_TXT_CONTACT=

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
# Graph-Versand mit SMTP-Rückfallebene (Feature 102, empfohlen ab Ende 2026 —
# SMTP-Basic-Auth-Abschaltung): MAIL_MAILER=failover + Kette wie unten.
# MAIL_FAILOVER_MAILERS=msgraph,smtp,log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

VAPID_SUBJECT=mailto:admin@example.com
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
LEGACY_WRITE_ENABLED=false

WORK_HOURS_CORE_START=08:00
WORK_HOURS_CORE_END=16:00
WORK_HOURS_CORE_DAYS=1,2,3,4,5

WORK_HOURS_EXTENDED_START=06:00
WORK_HOURS_EXTENDED_END=19:00
WORK_HOURS_EXTENDED_DAYS=1,2,3,4,5

# ── Stundenzettel / Material / Gleitzeit ─────────────────────────────
TIMESHEET_DEFAULT_WEEKLY_MINUTES=2400
TIMESHEET_DEFAULT_DAILY_MINUTES=480
TIMESHEET_DEFAULT_WORKING_DAYS=1,2,3,4,5
TIMESHEET_CORE_START=09:00
TIMESHEET_CORE_END=15:00
TIMESHEET_FRAME_START=06:00
TIMESHEET_FRAME_END=20:00
TIMESHEET_BREAK_AFTER=360
TIMESHEET_BREAK_MIN=30
TIMESHEET_SIGNATURE_MAX_KB=1000
TIMESHEET_MAGIC_MINUTES=1440
LEXOFFICE_API_KEY=

# Microsoft 365 (Msgraph-Plugin) — Instanz-App-Registrierung in Entra ID.
# Organisationen können im Plugin-Dialog eine eigene App hinterlegen (Variante B);
# ohne Overlay gilt diese Instanz-App. Multi-Tenant-Betrieb ("Mit Microsoft
# verbinden" ohne kundenseitige App-Registrierung): MSGRAPH_TENANT=common +
# publisher-verifizierte Entra-App — Konzept/Betriebsschritte:
# WorkDiary-Architecture/ms365-multitenant-app-2026-08.md. Redirect-URIs zeigt
# das Msgraph-Admin-Panel; Endpunkte/Scopes sind bewusst nur per config änderbar.
MSGRAPH_ENABLED=false
MSGRAPH_CLIENT_ID=
MSGRAPH_CLIENT_SECRET=
MSGRAPH_TENANT=common

# Routing / Touren (Nominatim Geocoding + OSRM Routing, optional selbst gehostet)
ROUTING_NOMINATIM_URL=http://localhost:8080
ROUTING_NOMINATIM_UA="workDiary/${APP_URL}"
ROUTING_NOMINATIM_EMAIL=admin@example.com
ROUTING_NOMINATIM_RATE=1
ROUTING_NOMINATIM_TIMEOUT=8
ROUTING_OSRM_URL=http://localhost:5000
ROUTING_OSRM_PROFILE=driving
ROUTING_OSRM_TIMEOUT=10
ROUTING_TILES_URL=https://tile.openstreetmap.org/{z}/{x}/{y}.png
ROUTING_TILES_MAX_ZOOM=19
ROUTING_CACHE_TTL_DAYS=365
ROUTING_TOUR_OPTIMIZER_ITERATIONS=50

WHISTLEBLOWING_KEY=
WHISTLEBLOWING_LOOKUP_KEY=

# Backup & Restore — Details: docs/backup-restore.md
# Heartbeat-Token des externen Backup-Skripts; erzeugen/rotieren mit
# `php artisan workdiary:backup:rotate-token` (leer = Endpoint deaktiviert).
BACKUP_HEARTBEAT_TOKEN=
# Frische-Schwelle der Statusseite je Quelle (Stunden).
BACKUP_HEARTBEAT_FRESHNESS_HOURS=26

# Verschlüsselte Cloud-Backupziele (Feature 017, Phase 32)
# Master-Key: 32 Byte base64 (php -r "echo base64_encode(random_bytes(32));")
# OFFLINE sichern — Verlust ohne Recovery-Key macht alle Backups wertlos.
BACKUP_MASTER_KEY=
# Optionaler Recovery-Public-Key (crypto_box, base64); ohne ihn warnt die UI dauerhaft.
BACKUP_RECOVERY_PUBLIC_KEY=

# --- Sicherheit: Integrität + Angriffserkennung (Features 095/096) ---
# Reverse-Proxy-IPs/CIDRs (Komma-Liste) oder '*'; ohne diese Angabe ist
# Request::ip() hinter einem Proxy die Proxy-IP (fail2ban/Limits/Geo!).
TRUSTED_PROXIES=
# Täglicher Quelltext-Integritätslauf (integrity:verify).
INTEGRITY_CHECK_ENABLED=true
# Realtime-Wächter (integrity:watch, MVP-453): Sammelfenster in Sekunden nach
# der letzten Änderung, bevor ein Verify ausgelöst wird. Braucht ext-inotify.
INTEGRITY_WATCH_DEBOUNCE=30
# Security-Log zusätzlich an Syslog spiegeln (SIEM-Anbindung).
SECURITY_LOG_SYSLOG=false
# Optionale IP-Allowlist (IPs/CIDRs) für Plattform-Admins im Adminbereich.
PLATFORM_ADMIN_IP_ALLOWLIST=
# Lokale GeoIP-Datenbank (.mmdb, MaxMind GeoLite2 oder DB-IP City Lite) für
# Sitzungs-Standortanzeige (085) und Impossible-Travel (MVP-449). Leer =
# beide Funktionen ruhen still. Bezug/Aktualisierung: docs/geoip.md
# (DB-IP Lite ist CC BY 4.0 — Attribution erforderlich).
GEOIP_DATABASE=
GEOIP_LOCALE=de

# --- Betrieb: Logging, Scheduler, Queue (Vollscan 2026-08-23, J13/J15) ---
# Produktion: LOG_STACK=daily + LOG_LEVEL=info (oben), sonst wächst eine
# einzige laravel.log im debug-Level ohne Rotation.
# Aufbewahrung der täglichen Logdateien in Tagen.
LOG_DAILY_DAYS=14
# Log-Kanal für die Toolkits (php-error-toolkit-Bridge); leer = Laravel-Default.
ERROR_TOOLKIT_LOG_CHANNEL=
# Aufbewahrung der Scheduler-Laufnachweise (scheduled_job_runs) in Tagen.
SCHEDULER_RUNS_RETENTION_DAYS=30
# Muss über dem größten Job-Timeout (600 s) liegen, sonst laufen Jobs doppelt.
DB_QUEUE_RETRY_AFTER=630
# Aufgelöste Integrations-Inbox-Einträge nach n Tagen löschen (integration:purge-inbox).
INTEGRATION_INBOX_RETENTION_DAYS=90
# Zustell-/Outbox-Protokolle: erfolgreiche nach n, gescheiterte nach m Tagen löschen.
INTEGRATION_DELIVERY_RETENTION_DAYS=90
INTEGRATION_FAILED_RETENTION_DAYS=180
# Nicht konvertierte Leads n Monate nach dem letzten Kontakt anonymisieren.
SALES_LEAD_RETENTION_MONTHS=6
# Umfrage-Ermüdungsschutz: keine zweite Einladung innerhalb von n Tagen.
SURVEYS_FATIGUE_DAYS=90
# Kundenrundschreiben: Empfänger je Queue-Lauf (Versand läuft in Batches).
CIRCULAR_BATCH_SIZE=200

# --- Mehrmandantenbetrieb (Sicherheitsscan 2026-08) -------------------------
# Im SaaS-Betrieb beide auf false setzen. Im Einzelplatzbetrieb bleiben sie an:
# dort steht in dieser Datei der eigene Schlüssel, und der eigene Server steht
# tatsächlich im eigenen Netz.
#
# false: Plugin-Geheimnisse fallen NICHT mehr auf diese Datei zurück — ein
#        Mandant ohne eigene Zugangsdaten arbeitet sonst über die des Betreibers.
#
# Vorgabe seit dem Sicherheitsaudit 2026-09-13: geschlossen, auch im Code.
# Eine Bestandsinstallation, deren Plugins auf diese Datei zurückfallen,
# verliert die Anbindung, bis die Organisation eigene Zugangsdaten hinterlegt
# oder hier ausdrücklich `true` steht.
PLUGINS_ALLOW_ENV_SECRET_FALLBACK=false
# false: Der Org-Admin kann die SSRF-Leitplanke nicht mehr je Anbindung
#        abschalten (CardDAV, JTL-Wawi, SSO).
PLUGINS_ALLOW_PRIVATE_NETWORK_OPT_IN=true

# Zeitzone, in der der Zeitplan (dailyAt/Cron der Job-Registry) ausgewertet wird
SCHEDULE_TIMEZONE=Europe/Berlin
# Betriebsfenster des Servers (HH:MM in SCHEDULE_TIMEZONE), z. B. 08:00 / 00:00 für
# nächtliche Abschaltung: Tageszeit-Jobs außerhalb laufen zu Fensterbeginn.
# Leer = rund um die Uhr. Auch unter Admin → Einstellungen übersteuerbar.
SCHEDULER_OPERATING_WINDOW_START=
SCHEDULER_OPERATING_WINDOW_END=

# Lieferantenkatalog per FTP ohne TLS zulassen (Vorgabe: false).
# Ohne TLS gehen Benutzername und Passwort im Klartext ueber die Leitung.
PROCUREMENT_FTP_ALLOW_PLAINTEXT=false

# Zusaetzliche Herkuenfte fuer Cross-Origin-Aufrufe der REST-Schnittstelle,
# kommagetrennt. Leer = nur APP_URL. Server-zu-Server und mobile Anwendungen
# senden keinen Origin und brauchen hier nichts (Sicherheitsaudit 2026-09-13).
CORS_ALLOWED_ORIGINS=

# SCORM-Kursinhalte von eigenem Host (Subdomain oder eigene Domain), damit fremder
# Kurscode nicht im Ursprung der Anwendung läuft. Leer = gleicher Ursprung
# (Systemcheck warnt). Voraussetzung: DNS, TLS und vHost-Alias auf dasselbe Docroot.
# Setzt zugleich das Präfix __Host- am Sitzungscookie (alle Sitzungen enden einmalig).
LEARNING_SCORM_CONTENT_URL=

# cmi5: So lange (Sekunden) nimmt das LRS nach dem Start einer AU Statements an.
LEARNING_CMI5_SESSION_TTL=28800

# ══════════════════════════════════════════════════════════════════
# Weitere Stellschrauben (nachgetragen 2026-09-17, MVP-796, Befund C1-13)
#
# Alle folgenden Schluessel werden von config/ gelesen, standen aber in
# keiner Vorlage — Betreiber konnten sie nicht kennen. Sie sind hier
# auskommentiert mit ihrer Vorgabe aufgefuehrt: Auskommentiert gilt die
# Vorgabe, Einkommentieren uebersteuert sie.
# ══════════════════════════════════════════════════════════════════

# ── Bewerbungen (config/applications.php) ───────────────────────────────────
# APPLICATIONS_REJECTED_RETENTION_MONTHS=6
# APPLICATIONS_TALENT_POOL_MONTHS=18

# ── Archivierung (config/archive.php) ──────────────────────────────────
# ARCHIVE_SCHEDULE_AT=03:00
# ARCHIVE_THRESHOLD_DAYS=30

# ── Anwesenheit (config/attendance.php) ───────────────────────────────────
# ATTENDANCE_SELF_CORRECTION=request

# ── Revisionsprotokoll (config/audit.php) ────────────────────────────
# AUDIT_RETENTION_YEARS=10

# ── Sicherung – Überwachung (config/backup.php) ───────────────────────
# BACKUP_HEARTBEAT_CRITICAL_HOURS=72
# BACKUP_HEARTBEAT_WARN_HOURS=26
# BACKUP_MIN_SIZE_BYTES=0
# BACKUP_RESTORE_TEST_OVERDUE_DAYS=180
# BACKUP_SIZE_DROP_RATIO=0.5

# ── Sicherung – Ziele und Werkzeuge (config/backup_targets.php) ───────────────
# BACKUP_DB_CONNECTION=
# BACKUP_FILES_ROOT=
# BACKUP_MYSQLDUMP_BINARY=mysqldump
# BACKUP_PART_SIZE=134_217_728
# BACKUP_PG_DUMP_BINARY=pg_dump
# BACKUP_RETENTION_DAILY=7
# BACKUP_RETENTION_MONTHLY=12
# BACKUP_RETENTION_WEEKLY=4
# BACKUP_TAR_BINARY=tar
# BACKUP_VERIFY_SAMPLE_PARTS=2
# BACKUP_WORK_DIR=

# ── Reklamationen (config/claims.php) ─────────────────────────────────
# CLAIMS_DEFAULT_DUE_DAYS=14
# CLAIMS_RECOURSE_RESPONSE_DAYS=14

# ── Cloud-Eingang (config/cloud_intake.php) ─────────────────────────────────
# CLOUD_INTAKE_MAX_FILE_SIZE=52_428_800
# CLOUD_INTAKE_MAX_PAGES=10

# ── Datenschutzmanagement (config/dataprotection.php) ─────────────────────────
# DATAPROTECTION_DSR_DEADLINE_DAYS=30
# DATAPROTECTION_DSR_REMINDER_LEAD_DAYS=7
# DATAPROTECTION_EXPIRY_WARNING_DAYS=30
# DATAPROTECTION_KEY=
# DATAPROTECTION_REVIEW_CYCLE_MONTHS=12

# ── Dokumentdesign (config/document_design.php) ────────────────────────────────
# DOCUMENT_DESIGN_DISK=local
# DOCUMENT_DESIGN_MAX_KB=10240
# DOCUMENT_DESIGN_MAX_PIXELS=40_000_000
# DOCUMENT_DESIGN_RENDER_DPI=150

# ── Auslagen (config/expenses.php) ──────────────────────────────────────
# EXPENSES_DEFAULT_CURRENCY=
# EXPENSES_DEFAULT_TAX_RATE=
# EXPENSES_MAX_UPLOAD_MB=10

# ── Exporte (config/exports.php) ───────────────────────────────────────
# TIME_EXPORT_DISK=local

# ── Feiertage (config/holidays.php) ─────────────────────────────────────
# HOLIDAYS_LOCALE=de_DE
# HOLIDAYS_PROVIDER=Germany\\Berlin

# ── Quelltext-Integrität (config/integrity.php) ──────────────────────────
# INTEGRITY_LOCKDOWN=off
# INTEGRITY_LOCKDOWN_SECRET=
# INTEGRITY_RETENTION_MONTHS=24

# ── Investitionen (config/investments.php) ─────────────────────────────────
# INVESTMENTS_APPROVAL_THRESHOLD=10000

# ── InvoicePlane-Plugin (config/invoiceplane.php) ───────────────────────────
# INVOICEPLANE_ENABLED=false
# INVOICEPLANE_HOST_ALLOWLIST=
# INVOICEPLANE_QUERY_TIMEOUT=5
# INVOICEPLANE_REQUIRE_TLS=true

# ── Fakturierung (config/invoicing.php) ──────────────────────────────────
# INVOICING_ASSEMBLY_HOURLY_RATE=
# INVOICING_DEFAULT_CURRENCY=EUR
# INVOICING_DEFAULT_HOURLY_RATE=
# INVOICING_DEFAULT_SERVICE_ARTICLE=
# INVOICING_DEFAULT_SERVICE_PLUGIN=lexoffice
# INVOICING_DEFAULT_TAX_RATE=19.00
# INVOICING_GIROCODE_ENABLED=false
# INVOICING_QUOTE_FOLLOW_UP_DAYS=7
# INVOICING_TIME_UNIT=h

# ── Lernplattform (config/learning.php) ─────────────────────────────────
# LEARNING_LTI_KEY_ROTATION_DAYS=90
# LEARNING_SCORM_TOKEN_TTL=28800

# ── Rechtliche Angaben (config/legal.php) ────────────────────────────
# LEGAL_IMPRINT=
# LEGAL_PRIVACY=

# ── Lizenzierung (config/license.php) ──────────────────────────────────
# LICENSE_CACHE_TTL=300
# LICENSE_DEV_HOSTS=127.0.0.1,localhost,::1
# LICENSE_DEV_HOST_ENVS=local,testing,development
# LICENSE_ENFORCE=true
# LICENSE_GRACE_DAYS=14
# LICENSE_KEY=
# LICENSE_KEY_PATH=license.key
# LICENSE_PRIVATE_KEY=
# LICENSE_PRIVATE_KEY_FILE=
# LICENSE_PUBLIC_KEY=gpiVanM0yrV6SPYq7q_HFBSjjS-DyZNdlbhj133-DOE
# LICENSE_SEAL_PATH=private/license-seal.php

# ── Standortzeiten (config/location.php) ────────────────────────────────
# LOCATION_DEFAULT_GAP_MERGE=10
# LOCATION_DEFAULT_MIN_DWELL=5
# LOCATION_DEFAULT_RADIUS_M=100
# LOCATION_RETENTION_DAYS=90

# ── Mail-Eingang (config/mail_intake.php) ──────────────────────────────────
# MAIL_INTAKE_MAX_ATTACHMENT_BYTES=25 * 1024 * 1024

# ── Medien und Transkription (config/media.php) ──────────────────────
# WHISPER_DEVICE=cpu
# WHISPER_MODEL=base
# WHISPER_MODEL_DIR=

# ── Benachrichtigungen (config/notifications.php) ────────────────────────────
# NOTIFICATIONS_PUSH_BODY_TRUNCATE=120
# NOTIFICATIONS_SMS_BODY_TRUNCATE=160
# NOTIFICATIONS_SMS_MONTHLY_LIMIT=250
# NOTIFICATIONS_SMS_WARN_PERCENT=80

# ── Seitengrößen der Listen (config/pagination.php) ───────────────────────
# PAGINATION_ARCHIVE=25
# PAGINATION_CUSTOMERS=25
# PAGINATION_CUSTOMER_ATTACHMENTS=20
# PAGINATION_CUSTOMER_SEARCH=10
# PAGINATION_DASHBOARD_RECENT=5
# PAGINATION_DUTY_PLANS=20
# PAGINATION_NOTIFICATIONS=25
# PAGINATION_ORGANIZATIONS=25
# PAGINATION_REMOTE_PENDING_GROUPS=10
# PAGINATION_REMOTE_SHARED_DEVICES=8
# PAGINATION_REMOTE_SHARED_SESSIONS=30
# PAGINATION_TAGS=50
# PAGINATION_TIMESHEETS=20
# PAGINATION_TOURS=25
# PAGINATION_VEHICLES=25

# ── Personenbeförderung (config/passenger.php) ───────────────────────────
# PASSENGER_MOBILITY_DATA_OBLIGATED=false
# PASSENGER_MOBILITY_SOLE_EXEMPT=true
# PASSENGER_TAX_REDUCED_MAX_KM=50

# ── Erweiterungen (config/plugins.php) ─────────────────────────────────
# PLUGINS_AUTO_DISABLE_BOOT_THRESHOLD=
# PLUGINS_AUTO_DISABLE_THRESHOLD=5
# PLUGINS_AUTO_DISABLE_WINDOW_HOURS=24
# PLUGINS_ERRORS_RETENTION_ACKNOWLEDGED_DAYS=30
# PLUGINS_ERRORS_RETENTION_OPEN_DAYS=90
# PLUGINS_HEALTH_EXCLUDE=
# PLUGINS_HEALTH_FLAP_THRESHOLD=2
# PLUGINS_HEALTH_TIMEOUT_SECONDS=10
# PLUGINS_PRIVATE_NETWORK_TARGETS=

# ── Datenschutz-Betriebsmodus (config/privacy.php) ─────────────────────
# PRIVACY_DPA_URL=
# PRIVACY_OPERATING_MODE=on_premise

# ── Beschaffung (config/procurement.php) ───────────────────────────────────
# PROCUREMENT_CREDENTIAL_BLOCKING=false

# ── Projekte (config/project.php) ──────────────────────────────────────
# PROJECT_DEFAULT_BILLABLE=
# PROJECT_DEFAULT_COLOR=#64748b
# PROJECT_DEFAULT_NAME=Wartung
# PROJECT_KEYWORD_MATCHING=true
# PROJECT_KEYWORD_MIN_LENGTH=4

# ── Auswertungen (config/reports.php) ──────────────────────────────────
# REPORTS_CSV_DELIMITER=;
# REPORTS_CSV_META_LINES=true
# REPORTS_CUSTOMER_ANALYSIS_CACHE_SECONDS=300

# ── Aufbewahrung (config/retention.php) ──────────────────────────────────
# RETENTION_DEFAULT_REGION=DE

# ── Karten und Routing (config/routing.php) ────────────────────────────
# ROUTING_TILES_ATTRIBUTION=

# ── Suche (config/search.php) ─────────────────────────────────────────
# SEARCH_ENGINE=auto
# SEARCH_INDEXING=true

# ── Sicherheit (config/security.php) ────────────────────────────────────
# SECURITY_EVENTS_RETENTION_DAYS=90
# SECURITY_IMPOSSIBLE_TRAVEL=true
# SECURITY_IMPOSSIBLE_TRAVEL_MAX_KMH=900
# SECURITY_IMPOSSIBLE_TRAVEL_MIN_KM=300
# SECURITY_TXT_EXPIRES_DAYS=180
# SECURITY_TXT_LANGUAGES=de, en
# SECURITY_TXT_POLICY=

# ── Krankmeldungen (config/sickness.php) ────────────────────────────────
# SICKNESS_ATTACHMENT_DISK=local
# SICKNESS_ATTACHMENT_FROM_DAY=4
# SICKNESS_ATTACHMENT_MAX_KB=10240
# SICKNESS_CHAIN_RESET_MONTHS=6
# SICKNESS_CONTINUED_PAY_WEEKS=6

# ── Kennungen (Sqids) (config/sqids.php) ─────────────────────────────
# SQIDS_ALPHABET=abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLM
# SQIDS_MIN_LENGTH=10
# SQIDS_SALT=

# ── Support (config/support.php) ───────────────────────────────────────
# SUPPORT_MODULE_CONTACT=info@workdiary.org

# ── Zuschläge (config/surcharge.php) ─────────────────────────────────────
# SURCHARGE_STACKING=highest

# ── Telemetrie (config/telemetry.php) ────────────────────────────────────
# TELEMETRY_ENABLED=true

# ── Stundenzettel, Fahrten und Reisekosten (config/timesheet.php) ────────
# LEXOFFICE_BASE_URL=https://api.lexoffice.io/v1
# TIMESHEET_BREAK_AUTO_APPLY=true
# TIMESHEET_DEFAULT_SCHEDULE_TYPE=flextime
# TIMESHEET_EDIT_WINDOW_DAYS=7
# TRAVEL_AUTO_TIME_ENTRY=true
# TRAVEL_RATE_BICYCLE_KM=0.05
# TRAVEL_RATE_COMPANY_KM=0.00
# TRAVEL_RATE_OTHER_KM=0.00
# TRAVEL_RATE_PRIVATE_KM=0.30
# TRAVEL_RATE_PUBLIC_KM=0.00

# ── Oberfläche (config/ui.php) ────────────────────────────────────
# UI_CALENDAR_SLOT_MINUTES=30
# UI_DASHBOARD_RECENT_LIMIT=5
# UI_NEWS_FEED_ENABLED=false
# UI_NEWS_FEED_URL=
# UI_SEARCH_RESULTS_LIMIT=20

# ── Upload-Grenzen (config/uploads.php) ────────────────────────────────
# UPLOAD_ATTACHMENT_KB=25600
# UPLOAD_CSV_IMPORT_KB=10240
# UPLOAD_CUSTOMER_ATTACHMENT_KB=10240

# ── Eingabegrenzen (config/validation.php) ────────────────────────────────
# VALIDATION_ATTENDANCE_BREAK_MAX=600
# VALIDATION_ATTENDANCE_DEVICE_MAX=64
# VALIDATION_ATTENDANCE_NOTE_MAX=1000
# VALIDATION_COMMENT_BODY_MAX=5000
# VALIDATION_DUTY_PLAN_NOTE_MAX=2000
# VALIDATION_TAG_NAME_MAX=60

# ── Hinweisgebersystem (config/whistleblowing.php) ────────────────────────────
# WHISTLEBLOWING_CLAMAV_BINARY=clamdscan
# WHISTLEBLOWING_EMERGENCY_TTL_MINUTES=240
# WHISTLEBLOWING_MAILBOX_SESSION_MINUTES=30
# WHISTLEBLOWING_RETENTION_MONTHS=36
# WHISTLEBLOWING_SCANNER=none
